INSTALLATION
create ngrok account
download ngrok and unzip
$ ./ngrok authtoken [token]

https://api.slack.com/apps/new
create bot user
install on slack server 'workshop'
invite bot to channel (iirc?)

pip install slackclient
export SLACK_BOT_TOKEN = [token]
gem install rubypython
gem install sinatra

RUN
ruby sina.rb
./ngrok http [port sina is running on]
(port is probably 4567)

set your webhook to the url from ngrok


note, the output onto the slack server right now is the entire postdata json that comes from the webhook, I don't know what to filter for, for now.
